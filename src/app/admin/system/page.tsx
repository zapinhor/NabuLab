import { AdminShell } from "@/components/admin/admin-shell"; import { requirePlatformAdmin } from "@/lib/admin/auth";
export default async function SystemPage(){const {role}=await requirePlatformAdmin({superAdmin:true,aal2:true}); const checks=[["Supabase server",Boolean(process.env.SUPABASE_SECRET_KEY??process.env.SUPABASE_SERVICE_ROLE_KEY)],["Hotmart webhook",Boolean(process.env.HOTMART_HOTTOK)],["Turnstile",Boolean(process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY)],["GA4 Data API",Boolean(process.env.GA4_PROPERTY_ID&&process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL&&process.env.GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY)],["GA4 Measurement Protocol",Boolean(process.env.GA4_API_SECRET)]];return <AdminShell title="Sistema" description={`Acesso sensível · ${role}. Valores secretos nunca são exibidos.`}><section className="grid gap-3 sm:grid-cols-2">{checks.map(([label,ok])=><article key={String(label)} className="rounded-xl border bg-white p-4"><b>{label}</b><p className={ok?"text-emerald-700":"text-amber-700"}>{ok?"Configurado":"Não conectado"}</p></article>)}</section></AdminShell>}
